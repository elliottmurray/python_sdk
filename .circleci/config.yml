jobs:
  publish:
    executor: python/default
    steps:
      - checkout
      - python/dist
      - run: pip install twine pytest && twine upload
orbs:
  python: circleci/python@1.0.0
version: 2.1
workflows:
  main:
    jobs:
      - python/test:
          pkg-manager: pip
          test-tool: pytest
      - publish